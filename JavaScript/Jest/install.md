# JEST 설치(공식 문서 참조)

## **_Install_**

1. Install jest using `yarn`

   - ```javascript
     yarn add --dev jest
     ```

1. Install jest using `npm`
   - ```javascript
     npm install --save-dev jest
     ```
